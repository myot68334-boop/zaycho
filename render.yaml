services:
  - type: web
    name: zaycho
    runtime: python
    rootDir: zaycho_backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
